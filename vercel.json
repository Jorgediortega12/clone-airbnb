{
    "build": {
      "env": {
        "PRISMA_CLIENT_ENGINE_TYPE": "binary"
      },
      "outputDirectory": ".next",
      "commands": {
        "build": "prisma generate && next build"
      }
    }
  }